
import React from 'react';
import PackagingOrders from '@/pages/production/PackagingOrders';

// This is a wrapper component to maintain compatibility with routes
const ProductionPackaging = () => {
  return <PackagingOrders />;
};

export default ProductionPackaging;
