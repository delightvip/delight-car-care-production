
import React from 'react';
import { Navigate } from 'react-router-dom';
import SettingsPage from './settings/Settings';

// Redirect to the settings page in the new location
const Settings = () => {
  return <SettingsPage />;
};

export default Settings;
